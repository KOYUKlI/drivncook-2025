<?php
// DEPRECATED: Replaced by BaselineSeeder (profile: bulk). This legacy class is retained as a no-op stub
// to avoid breaking references while older branches merge. Remove after stabilization.
namespace Database\Seeders;
use Illuminate\Database\Seeder;
class BulkSeed extends Seeder
{
    public function run(): void
    {
        // Intentionally left blank.
    }
}
